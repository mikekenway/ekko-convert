{
  "name": "video-converter",
  "private": true,
  "version": "1.0.0",
  "description": "Go API server for video conversion",
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o bin/server ./cmd/server",
    "start": "./bin/server"
  }
}
